import 'package:flutter/material.dart';

class ClientsScoreTab extends StatelessWidget {
  const ClientsScoreTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('운동 점수 내용'),
    );
  }
}
